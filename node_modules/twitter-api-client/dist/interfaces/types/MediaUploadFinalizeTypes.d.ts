export default interface MediaUploadFinalize {
    media_id: number;
    media_id_string: string;
    size: number;
    expires_after_secs: number;
    video: Video;
}
export interface Video {
    video_type: string;
}
